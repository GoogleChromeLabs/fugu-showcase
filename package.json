{
  "scripts": {
    "start": "node index.mjs",
    "fix": "npx prettier . --write",
    "deploy": "npm run fix && npm start && git add . && git commit -a -m 'New release' && git push"
  },
  "dependencies": {
    "capture-website": "^2.3.0",
    "filenamify-url": "^3.0.0",
    "node-fetch": "^3.2.4",
    "unfurl.js": "^5.6.4",
    "@minify-html/js": "^0.8.0",
    "prettier": "^2.6.2",
    "sharp": "^0.30.4"
  }
}
